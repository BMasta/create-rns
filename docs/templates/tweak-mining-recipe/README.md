## Checklist
1. Copy the files from `kubejs` into `kubejs` in your game folder.
2. Tweak the recipe in `kubejs/server_scripts/main.js`

Default built-in mining recipes can be found [here](../../../src/generated/builtin_packs/default/create_rns_dynamic_data/data/create_rns/recipe)
